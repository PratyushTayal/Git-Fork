using UnityEngine;

public class Pratyush : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("My name is PRATYUSH");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
